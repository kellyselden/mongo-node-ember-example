import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:select-localized', 'SelectLocalizedView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
